import { Home } from "./views/Home";
import { Route } from "react-router";
import  {React } from "rec"

export const ApplicationRoutes = () => (
    
    <Route exact path="/A" component={Home}></Route>    
);